<header>
    <h2>{{model.business.name}}</h2>
    {{#if model.favorite}}
    <button {{action "unsave"}}>Unsave</button>
    {{else}}
    <button {{action "save"}}>Save</button>
    {{/if}}
</header>
<ul>
    <li>Url: {{model.business.url}}</li>
    <li class="alert-primary">Url: {{model.business.location.formatted_address}}</li>
</ul>

{{outlet}}