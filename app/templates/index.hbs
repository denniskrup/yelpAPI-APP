<div class="logo-container mx-auto">
    <p class="script mt-3"><span>Wish Dish</span></p>
    <p class="text-border text1">THOUSANDS OF</p>
    <p class="text-border text2">FOOD</p>
    <p class="text-border text3">OPTIONS</p>
    <p class="script"><span>Powered by Yelp</span></p>
</div>

<Input list="browsers" @enter={{action "searchTerm" }} class="search-bar-custom mt-5 ml-5 text-center" @value={{term}}  @placeholder="Enter business name here"/>
<datalist id="browsers" >
  {{#each model.business as |business|}}
  <option>{{business.name}}</option>
  {{/each}}
</datalist>

{{!-- <Input class="search-bar-custom mt-5 second-search-margin text-center" @value={{location}}  @placeholder="Enter desired location here"/>
<Input class="search-bar-custom mt-5 third-search-margin text-center" @value={{price}}  @placeholder="Enter desired price here"/>
 --}}


{{model.id}}   
{{model.alias}}
{{model.url}}

{{outlet}}